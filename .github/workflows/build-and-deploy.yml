name: Build React App on PR

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]  # runs on new or updated PRs

jobs:
  build:
    runs-on: self-hosted   # or 'ubuntu-latest' if you want GitHub-hosted runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build React app
        run: npm run build
